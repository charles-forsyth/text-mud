[project]
name = "text-mud-devops"
version = "0.1.0"
description = "DevOps tools for text-mud project"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "pyproject.toml"
